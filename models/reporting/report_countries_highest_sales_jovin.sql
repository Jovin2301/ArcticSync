SELECT 
    ORDERID, 
    ORDERDATE,
    PRODUCTID,
    SUM(QUANTITY) AS QUANTITY,
    SUM(PRODUCTPRICE) AS TOTAL_PRICE,
    totalSales,
    SHIPCOUNTRY,
    SHIPCITY,
    CATEGORYNAME
FROM {{ ref('stg_jovin') }}
GROUP BY 
    ORDERID,
    PRODUCTID,
    QUANTITY,
    totalSales,
    SHIPCITY,
    SHIPCOUNTRY,
    CATEGORYNAME,
    PRODUCTPRICE, 
    ORDERDATE